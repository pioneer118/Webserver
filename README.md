# Webserver
Designed and implemented a lightweight HTTP server based on the LINUX platform, using a multi Reactor multi-threaded concurrency model, providing efficient I/O reuse through epoll, and combining technologies such as automatic buffer growth, timers, and asynchronous logs to achieve high-performance and stable server operation.
